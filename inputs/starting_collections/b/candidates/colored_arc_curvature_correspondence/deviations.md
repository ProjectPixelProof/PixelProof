# Candidate record

No protocol deviations.

## Selection and ranking record

Two explicit ranking passes were completed before implementation. The proposal
pool and the survivor criticism are preserved in `selection.md` and
`provenance.json`.

The candidate uses four uniquely colored open arcs, equalized profile multisets,
randomized local positions, orientations, scales, and stroke widths, and a
two-color profile swap for the decoy. The image-only arm estimates centerline
bow from the raster rather than reading the scene or renderer state.
