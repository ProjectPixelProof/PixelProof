id = "remainder_tower_cards"
version = "0.1.0"
canvas = [512, 512]
card_count = 4
towers_per_card = 4
decisions = ["hourglass", "diamond", "falling blocks", "rising blocks"]
transform = "Order cards by dot count; reduce each tower height by divisibility by three; stack the four decoded rows."
