# Materialization required

This task template is not a real-agent packet. Run
`python -m scripts.materialize_foundry` to inject the frozen v0.2 playbook,
campaign task specification, and visible seed snapshot.
