schema_version = "1.4"
artifacts = ["/logs/artifacts/submission"]

[task]
name = "question-foundry/build-question-worlds"
version = "0.2.0"
description = "Build a frozen portfolio of executable latent-z visual-question candidates"
keywords = ["vlm", "foundry", "world-generation", "verification"]

[metadata]
category = "agentic-research"
difficulty_explanation = "Requires complete executable worlds, independent pixel oracles, and auditable evidence."
verification_explanation = "A separate offline verifier executes deterministic generation, protected gates, corruptions, and registry comparisons."

[agent]
timeout_sec = 3600.0

[verifier]
timeout_sec = 900.0

[environment]
allow_internet = false
build_timeout_sec = 600.0
cpus = 2
memory_mb = 4096
storage_mb = 10240
gpus = 0
