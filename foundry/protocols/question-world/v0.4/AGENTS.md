# Inner-builder contract: question-world v0.4

Read `TASK_SPEC.toml`, every file under `protocol/`, and both files under
`memory/` before proposing candidates.

The visible mechanism memory is part of the accumulated outer-loop state. Treat
it as an exclusion and contrast index, not as implementation code. A renamed
object, axis swap, color substitution, arity-only variant, or taxonomy-only
distinction is not novel.

Work only inside `/workspace` and `/logs/artifacts`. Do not inspect environment
variables, credentials, external repositories, sibling trials, hidden tests, or
the canonical checkout. Do not claim certification, novelty, or human approval.

Maintain the process trace and both required ranking passes. Final survivors must
come from preserved checkpoints and must satisfy candidate schema v0.4.0.
