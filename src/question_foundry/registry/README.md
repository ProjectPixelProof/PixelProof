# Registry directory

This directory is a placeholder; it contains no registry implementation. The fixed
comparison tables are in the top-level [registry/](../../../registry/README.md).
Campaigns write their own state and episode records under their output directory.
